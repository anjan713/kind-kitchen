# Optional only. demo.html still works without these values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
ENABLE_LLM=false
ENABLE_FRONTEND_LLM=false
# Optional natural-voice narration for cooking steps (falls back to the browser voice).
ENABLE_TTS=false
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=alloy
PORT=8787
