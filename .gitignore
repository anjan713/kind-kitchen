# ── Secrets (MOST IMPORTANT) ────────────────────────────────
# .env holds your real OpenAI API key. Never commit it.
.env
.env.local
.env.*.local
# .env.example is the safe, empty template — it stays committed.

# ── Node ────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/

# ── OS / editor cruft ───────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp

# ── Internal build notes (optional) ─────────────────────────
# These are AI/agent scaffolding docs, not meant for evaluators.
# Uncomment any you want to keep OFF GitHub:
AGENTS.md
IMPLEMENTATION_PROMPT.md
HANDOFF.md
MODEL_SELECTION_PROMPT.md